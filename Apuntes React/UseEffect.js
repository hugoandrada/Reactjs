/*--------------------------------------------------
1 - importar useEffect en el componente
    import React, { useState, useEffect } from "react";
-----------------------------------------------------

2 - crear la funcion useeffect

    useEffect(() => {
        //pasar una funcion en el interior
        //no se puede crear directamente una funcion async await
        //se puede pasar la funcion dentro.

     }, []); //podemos asignar dependencias o no
        //el array vacio hace que se ejecute solo la primera vez que
          se renderiza el componente

*/